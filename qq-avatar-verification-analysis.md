# QQBot OpenID 与 UIN 头像校验分析

## 1. 背景

QQ 官方 Bot 群聊事件提供的是用户 `openid`，而部分 Yunzai 插件需要传统数字 QQ 号（UIN）。QQBot-Plugin 当前通过比较两种标识对应的头像文件 MD5，校验用户提交的 UIN 是否与事件中的 OpenID 对应：

```text
OpenID 头像：
https://thirdqq.qlogo.cn/qqapp/{appid}/{openid}/{size}

UIN 头像：
https://q.qlogo.cn/g?b=qq&nk={uin}&s={size}
```

现有实现下载两张头像并直接比较文件 MD5。实测发现，该方法对 PNG 头像正常，但部分 JPEG 头像虽然视觉内容相同，文件 MD5 却不同，导致绑定误判。

本文记录相关样本、JPEG 二进制结构和适合当前群聊绑定场景的兼容方案。

## 2. 事件字段确认

普通群消息样本中，以下字段值一致：

```text
author.id
author.member_openid
author.union_openid
sender.user_id
_raw_user_id
```

因此，本次样本的失败原因不是插件取错 OpenID，而是两个头像接口返回的文件存在差异。

## 3. PNG 测试

### 3.1 640×640

| UIN | AppID | 文件大小 | MD5 | 结果 |
| --- | --- | ---: | --- | --- |
| `2742600395` | `102132283` | 497381 字节 | `9e7a9ba2bd021d52fa5dad7671f19cb7` | OpenID 与 UIN 完全一致 |
| `3841227331` | `102131395` | 574726 字节 | `45535960ec91c753e1937298395bd6bd` | OpenID 与 UIN 完全一致 |

对两组 PNG 进一步解析后确认：

- 文件大小一致；
- 整个文件二进制逐字节一致；
- PNG chunk 顺序一致；
- `IHDR`、`gAMA`、`cHRM`、`bKGD`、全部 `IDAT` 和 `IEND` 内容一致；
- 每个 chunk 的 CRC 一致；
- 没有额外私有 chunk 或尾部附加数据；
- 解码后的像素完全一致。

### 3.2 100×100

对 UIN `2742600395` 的 100×100 头像实测，OpenID 与 UIN 两个接口仍返回完全相同的 PNG，MD5 一致。

当前样本表明，PNG 头像在 640 和 100 两种尺寸下均可继续使用严格 MD5 校验。

## 4. JPEG 测试

以下三组 640×640 JPEG 均为同一头像，但文件 MD5 不同。表中数据是首次测试时的响应快照：

| UIN | OpenID 文件大小 | UIN 文件大小 | 差值 | OpenID MD5 | UIN MD5 |
| --- | ---: | ---: | ---: | --- | --- |
| `2282484968` | 61361 | 61952 | +591 | `ee24007ee21c5d6f573a926f4ccefe55` | `5a124a08ec36c59c70fa78f900b90a33` |
| `1006781285` | 40168 | 40759 | +591 | `b364d87b2e909954230626fa9507aa8e` | `d6ad1f6460bc1ab5122ccda03d700bbf` |
| `154907985` | 58011 | 58602 | +591 | `5b0daa965b732158747ba73295c36b06` | `aa3b18ba8bb84071dd86a8d9b9488c0e` |

三组头像均为 640×640 JPEG。解码后的内容高度相似：

| UIN | 不同颜色通道比例 | 平均通道误差 | 最大通道误差 | PSNR |
| --- | ---: | ---: | ---: | ---: |
| `2282484968` | 0.8914% | 0.0204 / 255 | 20 / 255 | 59.40 dB |
| `1006781285` | 0.3972% | 0.0112 / 255 | 18 / 255 | 60.27 dB |
| `154907985` | 0.4392% | 0.0146 / 255 | 20 / 255 | 58.71 dB |

这些误差远小于肉眼可辨别范围，可认定为同一头像经过轻微不同处理后的产物。

## 5. JPEG 二进制结构

### 5.1 共同结构

同一头像的 OpenID 和 UIN JPEG 中，以下段在对应样本内逐字节一致：

```text
SOI   JPEG 开始标记
APP0  JFIF 基础信息
APP2  ICC 色彩配置
DQT   量化表
SOF0  尺寸、通道和采样方式
DHT   哈夫曼表
SOS   扫描参数
EOI   JPEG 结束标记
```

需要注意，这些结构描述编码方式，不是身份签名。不同头像也可能由相同编码器生成并共享相同结构，因此不能只凭 JPEG 头部一致就证明头像相同。

### 5.2 UIN 版本额外包含 APP11

OpenID 版本结构：

```text
SOI → APP0 → APP2 → DQT → SOF0 → DHT → SOS → 压缩图像流 → EOI
```

UIN 版本结构：

```text
SOI → APP0 → APP2 → DQT → SOF0 → DHT → APP11 → SOS → 压缩图像流 → EOI
```

UIN 版本在 `SOS` 前固定插入一个 `FF EB`（APP11）段。三组样本的 APP11 总大小均为 596 字节，其 592 字节载荷结构如下：

| 载荷偏移 | 长度 | 内容 | 说明 |
| ---: | ---: | --- | --- |
| 0 | 4 | `00000001` | 版本号 1 |
| 4 | 4 | `fae93b12` | 固定魔数 |
| 8 | 12 | 每张图片不同 | 未知字段 |
| 20 | 4 | `00000238` | 内部数据长度 568 |
| 24 | 568 | 高熵二进制 | 未公开的私有数据 |

计算关系：

```text
4 + 4 + 12 + 4 + 568 = 592 字节载荷
592 + FFEB标记及长度字段 = 596 字节APP11段
```

没有查到 `fae93b12` 的公开格式定义。结合固定 APP11 和少量 DCT 块变化，它可能与 QQ 私有签名、指纹或不可见标记有关，但该用途没有官方依据，不能作为确定结论。

### 5.3 压缩图像流长度会发生小幅变化

首次测试时，三个 UIN JPEG 的压缩图像流均比对应 OpenID JPEG 少 5 字节：

| UIN | OpenID 压缩流 | UIN 压缩流 | 差值 |
| --- | ---: | ---: | ---: |
| `2282484968` | 60508 | 60503 | -5 |
| `1006781285` | 39313 | 39308 | -5 |
| `154907985` | 57142 | 57137 | -5 |

当时的文件总大小因此增加 591 字节：

```text
APP11增加596字节 - 压缩流减少5字节 = 总大小增加591字节
```

后续复测发现，压缩流减少量并不固定，文件总大小差已经同时出现 591、592 和 593 字节。例如 UIN `3554844641` 的压缩流仅减少 3 字节，最终文件增加 593 字节。部分旧样本的 OpenID JPEG 也在头像未变化的情况下改变了 MD5 或减少 1 字节。

即使从 UIN JPEG 删除整个 APP11，两份文件仍不会完全一致，因为压缩图像流本身也发生了轻微变化。文件大小差只能用于诊断，不能作为校验条件。

### 5.4 变化集中在少量 JPEG 8×8 块

| UIN | 不同像素比例 | 受影响8×8块比例 |
| --- | ---: | ---: |
| `2282484968` | 1.1794% | 2.5938% |
| `1006781285` | 0.6621% | 1.5156% |
| `154907985` | 0.6411% | 1.8438% |

JPEG 使用可变长度哈夫曼编码。少量 DCT 系数变化后，后续压缩位流可能大面积错位，因此不能根据压缩流原始字节的相同比例判断头像是否相同。

## 6. 100×100 JPEG 测试

对 UIN `1006781285` 的 100×100 JPEG 测试结果：

| 项目 | OpenID | UIN |
| --- | ---: | ---: |
| 文件大小 | 3290 | 3881 |
| 压缩流大小 | 2465 | 2460 |
| MD5 | `77074f74445c3aca8d36a20a8db06dea` | `84fe226011afef16752204e5e3ca6b57` |

该次缩略图样本满足：

```text
APP11增加596字节
压缩流减少5字节
文件总大小增加591字节
```

这只是该次响应的结果，不能推广为固定长度规则。

解码后的平均通道误差为 0.6371 / 255，PSNR 为 41.86 dB，视觉内容仍高度相似。由于缩略图像素较少，相同程度的 JPEG 微调会覆盖更高比例的像素，因此不适合要求逐像素相等。

## 7. 其他 UIN 头像接口

测试了以下接口：

```text
https://q.qlogo.cn/headimg_dl?dst_uin={uin}&spec=640&img_type=jpg
```

三个 JPEG 样本中，`headimg_dl` 与原 `/g` 接口返回逐字节相同的文件，包含相同 APP11，MD5 也完全一致。

以下参数形式同样返回相同 JPEG：

```text
img_type=jpg
img_type=png
省略 img_type
```

`img_type=png` 没有强制转换格式。由此可见，`headimg_dl` 和 `/g` 背后使用同一份 UIN 头像缓存产物，更换接口无法绕过 JPEG 差异。



## 8. 推荐校验策略

当前群聊绑定功能主要用于防止普通用户输错 QQ 号，不作为能够抵抗专门攻击者的强身份认证。在该使用场景下，可以采用轻量的严格白名单策略。

### 8.1 推荐流程

```text
下载OpenID头像和UIN头像
        ↓
检查HTTP状态、文件非空及有效图片类型
        ↓
整个文件MD5相同？
    ├─ 是 → 校验通过
    └─ 否
        ↓
两边是否都是JPEG？
    ├─ 否 → 校验失败
    └─ 是
        ↓
UIN文件是否满足当前QQ JPEG特征？
    - SOS前存在596字节APP11
    - APP11 version = 1
    - APP11 magic = fae93b12
    - APP11 innerLength = 568
    - 忽略该APP11后，从SOI到SOS结束的头部与OpenID完全一致
    ├─ 是 → 校验通过
    └─ 否 → 校验失败
```

为进一步降低误判，可以追加比较：

```text
删除UIN APP11后，比较SOI至SOS头部：
APP0 / APP2 / DQT / SOF0 / DHT / SOS 必须一致
```

### 8.2 为什么不只比较文件大小

单独使用以下条件过于宽松：

```text
Math.abs(uinSize - openidSize) === 591
```

两个不同 JPEG 也可能碰巧具有相同的大小差，而且实测大小差已经在 591、592、593 之间变化。校验 APP11 的长度、位置、`fae93b12` 魔数，并在忽略 APP11 后比较完整 JPEG 头部，几乎没有额外性能成本，也不依赖压缩流长度波动。

### 8.3 适用边界

该方案适用于：

- 防止普通用户输入错误 QQ 号；
- 保持现有 PNG 严格 MD5 行为；
- 兼容当前已观察到的 QQ JPEG 私有处理规则；
- 不引入图片解码或感知哈希依赖。

该方案不保证抵抗：

- 专门复制目标头像的用户；
- 针对校验规则构造文件的攻击者；
- QQ 未来再次调整头像编码流程。

不符合已知规则时应默认拒绝绑定，并保留主人强制绑定作为人工兜底。

## 9. 当前结论

1. OpenID 与 UIN 字段在已检查的普通群消息中一致，问题不是取错 OpenID。
2. PNG 头像在现有 640 和 100 样本中二进制完全一致，MD5 校验正常。
3. JPEG 样本均出现相同的 596 字节 APP11 结构，但压缩流变化量和最终文件大小差并不固定，已观察到 591、592、593 字节三种差值。
4. APP11 使用固定魔数 `fae93b12`，但其具体含义没有公开依据。
5. 两种 JPEG 解码后视觉内容高度相似，严格文件 MD5 会产生误判。
6. `headimg_dl` 与 `/g` 返回相同 UIN 头像文件，不能解决该差异。
7. 对当前群聊绑定场景，推荐保留 MD5 快速路径，并为符合严格 APP11 特征的 JPEG 增加兼容路径。
