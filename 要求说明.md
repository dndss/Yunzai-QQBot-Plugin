---

## 一、`e.user_id` 是什么

`e.user_id` 是**事件对象上标识“谁触发了这个事件”的字段**，对应框架里的“用户 ID”。

框架收到消息或事件时，由适配器在 `prepareEvent` 之前赋值。不同适配器来源不同：

| 适配器 | `e.user_id` 原始值 | 举例 |
|---|---|---|
| OneBot v11（野生） | 真实 QQ 号（纯数字） | `2742600395` |
| QQBot 官方 | openid（脱敏唯一字符串） | `606238D6C9BB0E10544D0C97FAD9D81E` |
| OPQBot | `SenderUin`，一般是 QQ UIN | `123456789` |
| Satori | `event.user.id`，平台用户 ID | 字符串或数字 |
| ComWeChat | 微信用户标识 | `wxid_xxxxx` |
| GSUIDCore | `raw.user_id` | 由上游传入 |
| stdin（本地模拟） | 框架模拟的本地 ID | `10001` |

**关键结论**：`e.user_id` 没有固定格式，完全取决于你用了什么适配器。

---

## 二、`e.self_id` 是什么

`e.self_id` 是**收到事件的机器人自身的 ID**，跟 `e.user_id` 不同：

| 适配器 | `e.self_id` |
|---|---|
| OneBot v11（野生） | 机器人自己的 QQ 号 |
| QQBot 官方 | 机器人自己的 appid |
| 其他适配器 | 对应平台下机器人的身份标识 |

框架通过 `this.bots[e.self_id]` 来查找对应的 bot 实例，然后挂到 `e.bot`。

---

## 三、`e.bot` 是什么

`e.bot` 是 **收到这个事件的那个 Bot 实例**，由 `this.bots[e.self_id]` 确定：

```js
e.bot === this.bots[e.self_id]
```

`e.bot` 上的常用方法：

| 方法 | 含义 |
|---|---|
| `e.bot.uin` | 机器人自己的账号 ID |
| `e.bot.nickname` | 机器人昵称 |
| `e.bot.self_id` | 同 `e.self_id` |
| `e.bot.pickFriend(user_id)` | 用当前 bot 给某个用户发消息 |
| `e.bot.pickGroup(group_id)` | 获取群对象 |
| `e.bot.pickMember(group_id, user_id)` | 获取群成员 |
| `e.bot.adapter` | 这个 bot 来自哪个适配器 |

**关键结论**：`e.bot` 决定了 `pickFriend` 等操作走哪个适配器。用野生 bot 的 `e.bot.pickFriend` 传的是 QQ 号；用官方 bot 的 `e.bot.pickFriend` 传的是 openid。

---

## 四、你的目标：统一 `e.user_id`

你想让 **插件无感知底层 ID 差异**，无论消息从哪个适配器来，`e.user_id` 始终是**真实 QQ 号（uin）**。

### 野生 bot（OneBot）

- `e.user_id` 本来就是真实 QQ 号 → **不需要改**
- `e.bot.pickFriend(e.user_id)` 传 QQ 号，野生 bot 认识 → **没问题**

### 官方 QQBot

- `e.user_id` 原本是 openid → **需要映射成真实 QQ 号**
- `e.bot.pickFriend(e.user_id)` 传真实 QQ 号，官方 bot 不认识 → **适配器内部要反向翻译回 openid**

---

## 五、映射方案

### 核心思路

```
野生 bot:  e.user_id = 真实 QQ 号 2742600395  ✅ 直接用
官方 bot:  e.user_id = openid A1B2C3...       ❌ 要翻译
            ↓ 适配器层映射
            e.user_id = 真实 QQ 号 2742600395  ✅ 插件看到统一 ID
            ↓ pickFriend 时反向翻译
            传 openid 给 QQBot API            ✅ 官方 bot 认识
```

### 映射关系存储（Redis）

```js
// 正向：openid → 真实 QQ 号
map:qqbot:<appid>:<openid> → "2742600395"

// 反向：真实 QQ 号 → openid（pickFriend 时用）
map:uin:2742600395 → JSON.stringify({ appid, openid })
```

### 映射如何建立（绑定流程）

```
用户 → 官方 bot 发 "绑定"
     bot 回复 "请输入你的 QQ 号"
用户 → 输入 QQ 号 "2742600395"
     bot 校验头像 MD5 是否一致
        头像 URL1: thirdqq.qlogo.cn/qqapp/<appid>/<openid>/640
        头像 URL2: q.qlogo.cn/g?b=qq&nk=2742600395&s=640
        MD5 一致 → 确认是本人
     bot 存入映射关系
```

### 适配器魔改

**收到事件时**（官方 QQBot 适配器）：

```js
onMessage(data) {
  const openid = data.user_id
  const appid  = data.self_id

  const realUin = await redis.get(`map:qqbot:${appid}:${openid}`)

  if (realUin) {
    data._raw_user_id = openid   // 保留原始 ID
    data.user_id      = realUin  // 替换为统一 QQ 号
  }

  this.bot.prepareEvent(data)
}
```

**pickFriend 时**（官方 QQBot 适配器）：

```js
pickFriend(userId) {
  // userId 现在可能是真实 QQ 号，要反向查 openid
  const raw = await redis.get(`map:uin:${userId}`)

  if (raw) {
    const { openid } = JSON.parse(raw)
    return this._callQQBotAPI(openid)
  }

  // 没有映射时，userId 可能是原始 openid，直接用
  return this._callQQBotAPI(userId)
}
```

---

## 六、最终插件无感效果

```js
main: async function(e) {
  const uid = String(e.user_id)   // 永远是真实 QQ 号

  // 存数据
  await redis.set(`score:${uid}`, 100)

  // 发消息
  await e.bot.pickFriend(e.user_id).sendMsg("你的分数是 100")

  // 查群成员
  const member = await e.bot.pickMember(e.group_id, e.user_id)
}
```

不管事件来自哪个适配器，上面代码都能工作。

---

## 七、总结表

| 概念 | 说明 |
|---|---|
| **`e.user_id`** | 事件触发者的用户 ID，格式取决于适配器 |
| **`e.self_id`** | 收到事件的机器人自身 ID |
| **`e.bot`** | 收到事件的那个 Bot 实例 = `this.bots[e.self_id]` |
| **野生 bot 的 `e.user_id`** | 真实 QQ 号（纯数字），直接当统一标准用 |
| **官方 bot 的 `e.user_id`** | openid（字符串），需要映射为真实 QQ 号 |
| **映射存储** | Redis：`map:qqbot:<appid>:<openid>` → uin，`map:uin:<uin>` → openid |
| **绑定方式** | 用户主动输入 QQ 号 + 头像 MD5 校验确认本人 |
| **适配器职责** | 收到事件时翻译 `e.user_id`，调用 API 时反向翻译 |
| **头像校验** | 对比 `thirdqq.qlogo.cn/qqapp/...` 和 `q.qlogo.cn/g?b=qq&nk=...` 的 MD5，用来验证用户身份 |
| **插件无感** | 插件写 `e.user_id` 永远是真实 QQ 号，`e.bot.pickFriend(e.user_id)` 永远有效 |